struct UserIdeaComplete{
    total_key: data_field,
    users_here: HashMap<field_key, User>;

}

impl UserIdeaComplete {
    fn new(start_user: &User){
        &self.user_ideas.put(User.total_key, start_user);
    }
    
}
