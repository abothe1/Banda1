struct UserIdeaGen {
    key: field_key,
    completes: HashMap<u64, UserIdeaComplete>;
}

impl UserIdeaGen {
    fn new(complete: &UserIdeaComplete, g_vals: data_field){
        &self.comepletes.put(complete.TotalKey)
    }
    fn coputeKeys(){
        for x in g_vals{
            key*=x;
        }
    }
}
